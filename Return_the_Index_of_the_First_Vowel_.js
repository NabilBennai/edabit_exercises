function firstVowel(str) {
  return str.search(/[aeiou]/i);
}

function firstVowelAlgorithmics(str) {
  for (let index = 0; index < str.length; index++) {
    let letter = str[index];
    if (
      letter == "a" ||
      letter == "e" ||
      letter == "i" ||
      letter == "o" ||
      letter == "u"
    ) {
      return i;
    }
  }
}
